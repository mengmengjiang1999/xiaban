# 运行与导出

更新日期：2026-09-23。当前默认入口为 **正式第一关 release，版本 1.0.0**。**P5 完成，P6 本地交付、Chrome 整关与 macOS 构建验证完成；真实 itch.io 托管待账号或项目页面。** Safari 26.6.2 仅完成真实 UI 烟测，不代表完整整关兼容。实际结果、包体与校验值见 [P5–P6 交付测试记录](P5-P6交付测试记录.md)，旧阶段通过数仍保留各自范围。

## 1. 当前环境

- 工程目录：`/Users/mmj/Projects/games/xiaban/game/`。
- Godot **4.7.2.stable.official.ed1daf0bf**，GDScript，Compatibility 渲染；Web 为单线程。
- `tools/godot.sh` 查找本机 Godot，也可用 `GODOT_BIN` 指定引擎可执行文件。
- 导出模板应位于 `~/Library/Application Support/Godot/export_templates/4.7.2.stable/`，版本须匹配。
- 以下命令均在项目根目录 `/Users/mmj/Projects/games/xiaban` 执行。

## 2. 正式第一关启动与操作

```bash
bash tools/godot.sh
```

默认启动 `game/scenes/release.tscn`，也可显式执行 `bash tools/godot.sh res://scenes/release.tscn`。编辑器命令为 `bash tools/godot.sh --editor`；导入 `game/project.godot` 后，按 F5 运行正式第一关。办公室和人物在运行时组装，编辑器初始树只显示入口节点。

| 操作 | 输入与当前行为 |
|---|---|
| 前进／倒退 | W / S，沿人物朝向；倒退不反转身体和镜头 |
| 左右转身 | A / D，可原地转身，不作横移 |
| 蹲下／尝试站起 | C；完整起身空间不足时维持蹲姿 |
| 冲刺 | 站姿按住 Shift + W；蹲姿需先 C 站起，Shift 不自动起身；脚步声可能引起领导注意 |
| 翻滚 | 蹲姿按 Space，朝开始时的人物正前方滚动；锁向且不能手动取消，受阻停位移并播完剩余动作，再恢复为蹲姿；动作安静但仍可能被看见 |
| 环绕观察 | 按住鼠标右键左右拖动；不改变身体朝向或 W/S 参照 |
| 调节镜距／快速回正 | 滚轮／F，回正保留所选距离 |
| 暂停、释放鼠标 | Esc |
| 恢复／重试 | 暂停界面明确继续；结算或暂停界面重新开始 |
| 游戏音量 | 菜单或暂停界面，0～100%；0 为静音 |
| 镜头灵敏度 | 菜单或暂停界面，0.5～2.0 倍 |

音量和灵敏度保存到本机配置；网页设置属于当前浏览器站点，清除站点存储后可能恢复默认。默认音量 65%、灵敏度 1.0 倍。环境／效果的音量设置不改变领导听觉逻辑；办公声、动作、注意与结算反馈为项目原创程序音效。

点击开始／继续后才申请鼠标捕获；Esc、失焦或失去捕获会暂停，重新聚焦不自动恢复或抢回鼠标。暂停同时冻结玩家、三位领导的姿态与独立计时；重新开始恢复起点和各自的初始工作进度。镜头跟随玩家实际位置与水平朝向，不继承动画头部高度或翻滚旋转。

路线依次经过工位组长、办公室主管、楼梯口经理。进入每段先到高柜后观察，等领导低头后再蹲着走向矮柜；到柜端或门口重新观察、等待时机，再用 A/D 摆正人物朝向后翻滚。高柜与矮柜之间并非连续遮挡，低姿态本身不保证安全。连接走廊交替转向，进入下一段后需重新观察。

领导低头时不观察，抬头动作提供提前线索，仅观察阶段启用视觉发现。黄色地面提示当前观察方向，并根据姿态和遮挡采样；最终发现仍由实际头胸射线决定。持续看见到确认阈值才失败，重新藏住清空未完成确认。冲刺声由三位领导分别判断距离，范围内才抬头转向记录声源；听见本身不判失败，也不追逐。一次反应中连续脚步不会无限重启抬头或实时追踪藏住的玩家。翻滚安静，但身体露出仍可能被发现。

开放桌底仅禁止通行，视线按实际桌面、桌腿和设备判断，不形成不可见的整块遮挡墙。正式版保留 P4b 的全部碰撞体和路线；墙面、木饰面、地毯、地砖及壁面装饰用于提升辨识，不改变掩体高度与通路。

### 可调默认参数

C、Shift、Space 及以下数值沿用经过阶段验证的实施默认，不记为用户最终定稿。正式版沿用 P3 完整控制，不使用 P2 的 1–6 动作展示键。

| 项目 | 当前默认 |
|---|---|
| 步行／蹲行／冲刺 | 1.65 / 0.75 / 3.8 米／秒 |
| 站立／蹲姿碰撞高度、半径 | 1.80 / 1.05 米，半径 0.35 米 |
| 翻滚距离／动作／恢复 | 2.4 米 / 0.85 秒 / 0.20 秒 |
| 翻滚完整空间包络 | 宽 1.06 × 高 1.55 × 长 2.20 米；蹲姿能容纳不等于能够翻滚，不钻桌底 |
| 抬头／低头过渡 | 0.7 / 0.5 秒 |
| 视觉确认 | 连续可见 0.35 秒；重新遮挡立即清空进度 |
| 声音关注 | 当前冲刺事件半径 8 米；完成抬头后查看记录声源 2.0 秒 |
| 视觉范围 | 7.5 米、水平视角 100° |

| 领导 | 办公表现 | 办公／观察时长 | 开局已办公时间 |
|---|---|---|---|
| 工位组长 | 棕色背心、圆镜；站立敲键盘 | 4.8 / 2.0 秒 | 0 秒 |
| 办公室主管 | 蓝色马甲、低束发；站立翻文件 | 5.6 / 2.5 秒 | 1.1 秒 |
| 楼梯口经理 | 深灰西装、领带、方镜；站立夹板书写 | 4.2 / 2.2 秒 | 2.0 秒 |

开局进度只缩短首次工作阶段，后续每轮使用完整办公时长；重试恢复相同初始进度。三位领导没有新增坐姿、巡逻或追逐。P5 正式衣装、办公姿态及整关画面已完成检查；修正服装叠加白斑和头皮穿插后，最终引擎测试再次通过。

## 3. 正式浏览器试玩与 Web 导出

```bash
bash tools/build_web.sh release
python3 tools/serve_web.py --directory builds/release-web --port 8772
```

服务运行时打开 [正式第一关本地试玩页](http://127.0.0.1:8772/)。服务器只绑定 `127.0.0.1`；Ctrl+C 停止。修改代码或资产后需重新导出并刷新，页面不会自动更新。请通过 HTTP 访问，不要双击 `index.html`。

| 产物或预设 | 用途 |
|---|---|
| `builds/release-web/index.html` | 正式第一关浏览器入口 |
| `builds/xiaban-release-web.zip` | 正式上传包路径，根目录包含入口、运行文件与许可说明 |
| `Web Release` | 使用 `release.tscn`，版本 `1.0.0` |
| `Web P4 Full` | 独立 `p4b` feature，保留 `p4b.tscn` 与版本 `0.6.0-p4` |
| `Web P4` | 独立 `p4` feature，保留 P4a 单领导场景 |
| `Web` / `Web P2` / `Web P3` | 分别保留 P1／P2／P3 独立场景 |

脚本检查 ZIP 完整性和根目录入口，并附字体及人物许可。最终 Web、ZIP 与原生包已核对同内容 PCK；结果见 [P5–P6 交付测试记录](P5-P6交付测试记录.md)，机器可读清单为 `builds/release-manifest.json`。当前未上传或公开发布；本地页面及 iframe 检查不能替代实际 itch.io 托管验收。

普通试玩地址不启用验收状态；自动化检查添加 `?p1_qa=1&p3_qa=1&p4b_qa=1&release_qa=1`，仅允许读取状态，不通过它移动玩家或改变领导。保持 8772 服务运行后执行：

```bash
node tools/test_browser_release.cjs
```

浏览器脚本使用已获用户授权的 Playwright 与 Google Chrome，依赖位于 `.tools/browser-qa/`。`RELEASE_HEADLESS=0` 使用有界面窗口，`RELEASE_URL` 改地址，`RELEASE_FINAL_ONLY=1` 复核最终包完整路线，`RELEASE_SMOKE_ONLY=1` 检查普通启动和本地 iframe。报告分别保存于 `.logs/release-browser/`、`.logs/release-browser-final/` 与 `.logs/release-browser-smoke/`。

正式包 Chrome 完整检查 **67／67 通过**，另轮菜单定向检查 **26／26 通过**，两轮不相加。实际键鼠通关 128.800 秒、58.414 米，分别由三位领导发现的失败流程均通过。三位领导正常运行的 8.0157 秒采样中记录 481 帧，平均 60.007 FPS，帧间隔 P95 为 17.6 ms、最大 17.7 ms；这是当前机器和采样窗口的结果，不是所有设备性能承诺。

真实 Safari **26.6.2** 完成加载、持续运行 56.37 秒、C 蹲起、Space 翻滚 2.4 米、Esc 暂停／继续、全屏进退及切标签暂停烟测。未做完整通关与失败；后期原生截图工具出现 `SC -3811`，普通页后续操作未能复核，因此不能宣布完整 Safari 整关兼容。Chrome、真实 Safari、摆位画面和同引擎 WebKit 测试的证据分别记载；Windows 无本次实机验证，手机触屏不支持。

若 Chrome 在载入游戏前的系统注册阶段退出，应区分启动环境故障与游戏运行崩溃；此前错误分类见 [P2 测试记录](P2测试记录.md)。

## 4. 正式 macOS 导出

```bash
mkdir -p builds/macos
bash tools/godot.sh --headless --export-release 'macOS Release' ../builds/macos/Xiaban.app
```

`macOS Release` 版本为 `1.0.0`，Universal、本地 ad-hoc 签名，未做 Apple 公证，非 Mac App Store 分发。最终 `Xiaban.app` 已通过 `codesign --verify --deep --strict`，并在本机 Apple M4 GPU 实际启动、输出 `RELEASE_READY` 后退出 0；该启动检查不等于全程桌面键鼠通关，也不代表 Intel Mac 或任意外部机器验证。

一键重建两种正式包并核对签名、PCK 一致性和 ZIP 完整性：

```bash
bash tools/build_release.sh
```

| 正式产物 | 大小／一致性 |
|---|---|
| `builds/xiaban-release-web.zip` | 24,901,169 B，约 23.75 MiB |
| `builds/xiaban-macos.zip` | 75,220,970 B；包含 `Xiaban.app` 与许可说明 |
| Web／macOS 共用 PCK | 14,730,148 B，SHA-256 `c249bd89ba0b895cb56c5ce5579f3e4c24f16a7391b50573c28641793b1ca6ef` |

完整包摘要由 `builds/release-manifest.json` 记录。正式截图在 `docs/images/release-*.png`，已目检的 1260 × 1000 宣传封面在 `art/release/xiaban-cover.png`；宣传摆位与真正输入通关各自留证。

## 5. 正式引擎与画面检查

```bash
bash tools/godot.sh --headless --fixed-fps 60 --script res://tests/release_test.gd
```

正式引擎测试复用整关物理路线、三领导独立感知、姿态遮挡、失败、暂停与重试，并加入正式角色、场景、声音与设置等检查。最终衣装落定后再次运行，**92／92 通过、退出码 0**。该结果与最终包 Chrome、原生画面和签名核验共同构成本地交付证据；实际 itch.io 托管仍待账号或页面。

原生画面脚本为 `res://tests/release_visual.gd`。摆位截图用于检查服装、动作和构图，不计作实际键鼠通关；最终帧率、包体和当前机器证据见 [P5–P6 交付测试记录](P5-P6交付测试记录.md)。

基础人形与六段动画的来源及重建方式见 [人物资源选型](人物模型与动画资源选型.md)与 [角色源文件](../art/characters/README.md)。新增服装附件、办公动作、场景微纹理与声音为项目原创；编辑源文件位于 `art/characters/`，不进入游戏导出，资源许可与项目代码许可分别管理。

## 6. 保留的 P1～P4b 与性能基线

| 版本 | 启动场景 | Web 构建命令 | 服务目录／端口 | macOS 预设／应用 |
|---|---|---|---|---|
| P1 胶囊与镜头 | `res://scenes/p1.tscn` | `bash tools/build_web.sh p1` | `builds/web` / 8766 | `macOS` / `XiabanP1.app` |
| P2 人形与动作样板 | `res://scenes/p2.tscn` | `bash tools/build_web.sh p2` | `builds/p2-web` / 8767 | `macOS P2` / `XiabanP2.app` |
| P3 完整控制 | `res://scenes/p3.tscn` | `bash tools/build_web.sh p3` | `builds/p3-web` / 8769 | `macOS P3` / `XiabanP3.app` |
| P4a 单领导小关 | `res://scenes/p4.tscn` | `bash tools/build_web.sh p4` | `builds/p4-web` / 8770 | `macOS P4` / `XiabanP4.app` |
| P4b 三领导完整路线 | `res://scenes/p4b.tscn` | `bash tools/build_web.sh p4b` | `builds/p4b-web` / 8771 | `macOS P4 Full` / `XiabanP4Full.app` |

例如运行保留的 P4a：

```bash
bash tools/godot.sh res://scenes/p4.tscn
bash tools/build_web.sh p4
python3 tools/serve_web.py --directory builds/p4-web --port 8770
node tools/test_browser_p4.cjs
```

`bash tools/build_web.sh` 不带参数默认导出正式 release；服务脚本不带参数仍默认目录 `builds/web`、端口 8765。明确指定目录和端口可避免打开错误阶段。P4a 原记录中的 41 项引擎、30 项 Chrome 和 23.68 MiB ZIP 保持为该阶段历史证据，不替代正式 release 验收。P4b 同样保留独立 `p4b` feature 和 `Web P4 Full`／`macOS P4 Full` 预设；其 69 项引擎、52 项 Chrome 完整交互基线与最终包复核属于 [P4 完整关卡历史记录](P4完整关卡测试记录.md)。

P3 保留完整动作、碰撞和声音事件，但不接入领导 AI。`node tools/test_browser_p3.cjs` 默认使用 8769，`P3_HEADLESS=0` 可开启有界面窗口，`P3_URL` 可改地址，报告位于 `.logs/p3-browser/`；P3 历史结果见 [P3 测试记录](P3测试记录.md)。P2 保留正常 W/S 行走与数字 1–6 六段原地展示，0 返回行走；原地展示不改变站立碰撞或产生动作位移。`node tools/test_browser_p2.cjs` 使用 8767，`P2_HEADLESS=0` 可开启有界面窗口，`P2_URL` 可改地址，`P2_ASSET_ONLY=1` 仅做素材定向复验。

保留的引擎回归：

```bash
bash tools/godot.sh --headless --fixed-fps 60 --script res://tests/p4b_test.gd
bash tools/godot.sh --headless --fixed-fps 60 --script res://tests/p4_test.gd
bash tools/godot.sh --headless --fixed-fps 60 --script res://tests/p3_test.gd
bash tools/godot.sh --headless --fixed-fps 60 --script res://tests/p3_roll_grounding_test.gd
bash tools/godot.sh --headless --fixed-fps 60 --script res://tests/stance_collision_test.gd
bash tools/godot.sh --headless --fixed-fps 60 --script res://tests/p2_test.gd
bash tools/godot.sh --headless --fixed-fps 60 --script res://tests/p1_test.gd
```

P3 阶段历史通过数为 72 项控制、18 项蒙皮接地和 22 项姿态空间；P2 为 61 项，P1 基础为 86 项。正式包的实际重跑结果另记入 [P5–P6 交付测试记录](P5-P6交付测试记录.md)，不用历史数字冒充新结果。

同模渲染基线保留独立入口，不包含领导 AI：

```bash
mkdir -p builds/p2-benchmark
bash tools/godot.sh --headless --export-release 'Web P2 Benchmark' ../builds/p2-benchmark/index.html
python3 tools/serve_web.py --directory builds/p2-benchmark --port 8768
```

该预设以 `p2_benchmark` 固定 `res://tests/p2_performance.tscn`，不随当前默认场景切换。P1、P2 的验证范围分别见 [P1 测试记录](P1测试记录.md)和 [P2 测试记录](P2测试记录.md)。

## 7. v0.1 历史版本

旧斜俯视 v0.1 可显式运行 `bash tools/godot.sh res://scenes/main.tscn`，测试入口为 `res://tests/level_test.gd` 和 `res://tests/playthrough.gd`。其追赶、抓捕、世界坐标移动属于 [旧版测试记录](第一关测试记录.md) 的历史规则，不能用于当前新规则验收。[第一关试玩说明](第一关试玩说明.md) 现已更新为正式 release 操作。
